/**
 * enum of the supported LogLevel
 */
export const enum LogLevel {
  // Level goes from very verbose to quiet
  Verbose = 0,
  Debug = 1,
  Info = 2,
  Warning = 3,
  Error = 4,
  Critical = 5,
  Quiet = 6,
}
