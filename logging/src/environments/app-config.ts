/*
 * Configuration for the logging test app
 *
 * Although the AppConfig interface plays no role in dependency injection, it supports typing of the configuration object within the class.
 *
 * */

export interface AppConfig {

    production: boolean;

}

